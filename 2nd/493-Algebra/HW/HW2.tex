\documentclass[12pt, letterpaper]{article}

%-------Packages---------
\usepackage{amssymb,amsfonts}
\usepackage{mathptmx}
\usepackage{amsthm}
\usepackage{graphicx}
\usepackage[all,arc]{xy}
\usepackage[colorlinks=true, allcolors=blue]{hyperref}
\usepackage{enumerate}
\usepackage{bbm}
\usepackage{dsfont}
\usepackage{mathrsfs}
\usepackage{tikz-cd}
\usepackage{setspace}
\usepackage{import}
\usepackage[utf8]{inputenc}
\usepackage{hyperref}
\usepackage{tikz}
\usepackage{float}
\usepackage{mdframed}
\usepackage{fancyhdr}
\usepackage[nointegrals]{wasysym}

\usepackage[left=1in,top=1in,right=1in,bottom=1in]{geometry}

\usepackage{mathtools}
\usepackage{setspace}
\usepackage{cleveref}
\usepackage{multicol}

%--------Theorem Environments--------
%theoremstyle{plain} --- default
\newmdtheoremenv{theorem}{Theorem}[subsection]
\newmdtheoremenv{corollary}[theorem]{Corollary}
\newtheorem{proposition}[theorem]{Proposition}
\newmdtheoremenv{lemma}[theorem]{Lemma}
\newtheorem{conj}[theorem]{Conjecture}
\newtheorem{quest}[theorem]{Question}

\theoremstyle{definition}
\newmdtheoremenv{definition}[theorem]{Definition}
\newmdtheoremenv{definitions}[theorem]{Definitions}
\newtheorem{example}[theorem]{Example}
\newtheorem{algorithm}[theorem]{Algorithm}
\newtheorem{rmk}[theorem]{Remark}
\newtheorem{exercise}[theorem]{Exercise}

%----------- my commands -------------
\newcommand{\C}{\mathbb{C}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\N}{\mathbb{N}}
\renewcommand{\P}{\mathbb{P}}
\newcommand{\contr}{\Rightarrow \Leftarrow}
\newcommand{\HRule}[1]{\rule{\linewidth}{#1}}
\newcommand{\s}{\(\,\)}
\renewcommand{\t}[1]{\text{#1}}
\newcommand{\ang}[1]{\left\langle #1 \right\rangle}

% Meta data
\setstretch{1.2}

\begin{document}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[LOE]{\slshape{\textbf{Vincent Ferrara}}}
\fancyhead[ROE]{\thepage}

\section*{HW 1}

\subsection*{Problem 1}
Let $G$ be a group and $N \unlhd G$\\
Let $\psi$ be an homomorphism form $G$ to $G_1$ s.t. $N \subseteq \text{ ker}(\psi)$\\
Define: $\bar{\psi}: G \diagup N \rightarrow G_1$ s.t. $\bar{\psi}(gN)=\psi(g)$\\\
Well Defined: Suppose $gN=hN \implies gh^{-1} \in N \implies gh^{-1} \in $ ker$(\psi)$\\
$\psi(gh^{-1})=e \implies \psi(g)=\psi(h)$ since $\psi$ is a homomorphism this implies $\bar{\psi}(gN)=\bar{\psi}(hN)$\\
Homomorphism: Let $gN,hN \in G \diagup N$\\
$\bar{\psi}(gNhN)=\bar{\psi}(ghN)=\psi(gh)=\psi(g)\psi(h)=\bar{\psi}(gN)\bar{\psi}(hN)$\\
Uniqueness: Suppose there exists another homomorphism $\phi: G\diagup  N \rightarrow G_1$ such that $\varphi=\psi \circ \varphi$\\
\[
\phi(gN)=\psi(g) \implies \phi = \bar{\psi}
\]

\newpage
\subsection*{Problem 2}
Let $H$ be a subgroup of $G$.
\begin{itemize}
    \item (Inverses) Let $a \in N_G(H)$\\
    Consider: $a^{-1}Ha=a^{-1}aH=H$ thus $a^{-1} \in N_G(H)$
    \item (Closure) Let $a,b \in N_G(H)$\\
    Consider: $abHb^{-1}a^{-1} = abb^{-1}Ha^{-1}=aHa^{-1}=H$ thus $ab \in N_G(H)$
    \item (Identity) Consider $eHe=H$ thus $e \in N_G(H)$
\end{itemize}
Let $h \in H$\\
Consider : $hHh^{-1}$ since $h,h^{-1} \in H \implies hHh^{-1}=H$ thus $h \in N_G(H)$ thus $H \subseteq N_G(H)$\\
By definition since $\forall g \in N_g(H)\; gHg^{-1}=H$ thus $H \unlhd N_G(H)$\\
Largest: Let $K$ be a subgroup of $G$ s.t. $H \unlhd K$ thus $\forall k \in K$ 
\[kHk^{-1}=H \]
thus $k \in N_G(H)$ thus $K \subseteq N_G(H)$

\newpage
\subsection*{Problem 3}
\begin{itemize}
    \item (i)
    \begin{proof}
        \phantom{text}
        \begin{itemize}
            \item (Closure) Let $hk,jl \in HK$ s.t. $h,j \in H$ and $k,l \in K$\\
            $hkjl=h(kj)l$ since $H \subseteq N_G(K) \implies kj=jk'$ for $k' \in K$\\
            $h(jk')l=hjk'l \in HK$
            \item (Identity) Since $H,K$ are subgroups of $G$, $e\in H\cap K \implies ee=e\in HK$
            \item (Inverses) Let $hk \in HK$ s.t. $h\in H$ and $k \in K$\\
            Consider $k^{-1}h^{-1}$ since $H \subseteq N_G(K) \implies k^{-1}h^{-1}=h^{-1}k'$ for $k' \in K$\\
            thus $k^{-1}h^{-1}=h^{-1}k'\in HK$\\
            \phantom{text}\\
            \item Let $hk \in HK$ s.t. $h\in H$ and $k,k' \in K$\\
            $(hk)k'(hk)^{-1}=h(kk'k^{-1})h^{-1}$ thus $kk'k^{-1}=l \in K$ and since $h \in H \subseteq N_G(K)$\\ 
            $\implies hlh^{-1} \in K$ thus $(hk)k'(hk)^{-1} \in K$ thus $K \unlhd HK$
        \end{itemize}
    \end{proof}
    \item (ii)
    \begin{proof}
        Let $l \in H \cap K \implies l \in H$ and $l \in K$ and $h \in H$\\
        $hlh^{-1}$ since $l \in K$ and $H \subset N_G(K) \implies hlh^{-1} \in K$\\
        since $h \in H$ and $l \in H \implies hlh^{-1} \in H$ thus $hlh^{-1} \in H \cap K$ thus $H\cap K \unlhd H$
    \end{proof}
    \item (iii)
    \begin{proof}
        Let $\varphi: H \rightarrow HK \diagup K$ s.t. $\varphi(h)=hK$\\
        Well-defined: Suppose $h_1 = h_2 \in H \implies \varphi(h_1)=h_1K=h_2K=\varphi(h_2)$\\
        Homomorphism: Let $h,j \in H$\\
        $\varphi(hj)=hjK=hKjK=\varphi(h)\varphi(j)$\\
        Kernel: Let $h \in \text{ker}(\varphi) \unlhd H \implies h \in H$\\
        $\varphi(h)=hK=K \implies h \in K \implies h \in H \cap K \implies$ ker$(\varphi)=H \cap K$\\
        Surjective: Let $hkK \in HK \diagup K$, for $h \in H,\; k\in K$\\
        $\varphi(h)=hK=hkK$ thus im$(\varphi)=HK \diagup K$\\
        Thus by the first isomorphism theorem $H \diagup H \cap K \cong HK \diagup K$
    \end{proof}
\end{itemize}

\newpage
\subsection*{Problem 4}
\begin{enumerate}[(a)]
    \item \begin{itemize}
        \item (i. $\implies$ ii.)\\
        Assume i.\\
        Let $g \in G$ by i. we know $g=hk$ for $h \in H$ and $k \in K$ thus $g=hk\in HK$\\
        Let $hk \in HK$ s.t. $h \in H$ and $k \in K$ since $H,K \subseteq G \implies hk \in G$ thus $G=HK$\\
        Since $e\in H,K \implies e \in H \cap K \implies \{e\} \subseteq H \cap K$\\
        Let $l \in H \cap K \implies l \in H$ and $l \in K$ and $g \in G$ by i. $g=hk$ for $h \in H$ and $k \in K$\\
        $g=hxx^{-1}k$ thus $hx \in H$ and $x^{-1}k \in K$ thus by i. since we assume uniqueness for each $g \in G$ this implies $x=e$ thus $H \cap K = \{e\}$
        \item (ii. $\implies$ iii.)\\
        Assume ii.\\
        Define: $\varphi: H \times K \rightarrow G$ s.t. $\varphi(h,k)=hk$\\
        Homomorphism: Let $(h,k),(j,l) \in H \times K$\\
        $\varphi((h,k)(j,l))=\varphi(hj,kl)=hjkl=\varphi(hj)\varphi(kl)$\\
        Injective: Let $(h,k) \in$ ker$(\varphi)$\\
        $\varphi(h,k)=hk=e \implies h=k^{-1} \implies h,k \in H \cap K$ by ii. $\implies h=k=e$\\
        Thus $(h,k)=(e,e) \implies$ ker$(\varphi)=\{(e,e)\} \implies \varphi$ is injective.\\
        Surjective: Let $g \in G$ by ii. $g=hk$ for $h \in H$ and $k \in K$ thus $\varphi(h,k)=hk=g$ thus surjective\\
        Thus $\varphi$ is an isomorphism
        \item (iii. $\implies$ i.)\\
        Assume iii.\\
        Let $g \in G$ and $k \in K$\\
        By iii. since $H \times K$ is isomorphic to $G$ this implies for all $g \in G$
        can be written uniquely as a product $hk$, with $h \in H$ and $k \in K$\\
        Let $g \in G$ and $h_1 \in H$\\
        $gh_1g^{-1}$ by iii. $g=hk$ for $h \in H$ and $k \in K$\\
        $hkh_1k^{-1}h^{-1}$\\
        Consider $kh_1k^{-1} \in G \implies kh_1k^{-1}=k\varphi(h_1,k^{-1})=k\varphi((e,k^{-1})(h_1,e))=k\varphi(e,k^{-1})\varphi(h_1,e)=kk^{-1}h_1=h_1 \in H$\\
        Thus $kh_1k^{-1}=h_1 \in H \implies gh_1g^{-1} \in H$ thus $H$ is normal in $G$\\
        Let $g \in G$ and $k_1 \in K$\\
        $gk_1g^{-1}$ by iii. $g=hk$ for $h \in H$ and $k \in K$\\
        $hkk_1k^{-1}h^{-1}$. Let $l=kk_1k^{-1}$\\
        $hlh^{-1}=\varphi(h,l)h^{-1}=\varphi(e,l)\varphi(h,e)h^{-1}=lhh^{-1}=l \in K \implies K \unlhd G$
    \end{itemize}
    \item If $G$ is an internal direct product of $H$ and $K$ then $G \cong H \times K$
\end{enumerate}

\newpage
\subsection*{Problem 5}
\begin{enumerate}[(a)]
    \item $\varphi: \{\text{subgroups of $G$ containing $N$}\} \rightarrow \{\text{subgroups of $G \diagup N$}\}$ s.t. $\varphi(H)=\phi(H)=H\diagup N$\\
    $\varphi^{-1}: \{\text{subgroups of $G \diagup N$}\} \rightarrow \{\text{subgroups of $G$ containing $N$}\}$ s.t. $\varphi^{-1}(\bar{H})=\phi^{-1}(\bar{H})$\\
    Let $H \leq G$\\
    $\varphi^{-1}(\varphi(H))=\varphi^{-1}(H \diagup N)=H$\\
    Let $\bar{H} \leq G \diagup N$\\
    $\varphi(\varphi^{-1}(\bar{H}))=\varphi(\phi(\bar{H}))=\bar{H}$\\
    Thus $\varphi$ is bijective since it has an inverse\\
    Let $H \unlhd G$ and $H \supseteq N$ consider $\varphi(H)=H \diagup N$\\
    Let $gN \in G \diagup N$ and $hN \in H \diagup N$\\
    $gNhNg^{-1}N=ghg^{-1}N$ since $H \unlhd G \implies ghg^{-1} \in H \implies gNhNg^{-1}N=ghg^{-1}N \in H \diagup N$ \\
    Let $\bar{H} \unlhd G \diagup N$ consider $\varphi^{-1}(\bar{H})$\\
    Let $g \in G$ and $h \in \varphi^{-1}(\bar{H})$\\
    $\phi(ghg^{-1})=ghg^{-1}N \in \bar{H}$ since $\bar{H}$ is normal $ \implies ghg^{-1} \in \varphi^{-1}(\bar{H}) \implies \varphi^{-1}(\bar{H}) \unlhd G$
    \item Let $h \in H$ and $n \in N$\\
    $hnh^{-1}$ since $h \in H \subseteq G$ and $N \unlhd G \implies hnh^{-1} \in N \implies N \unlhd H$\\
    By (a) since $H \unlhd G \implies H \diagup N \unlhd G \diagup N$\\
    Let $\varphi: G \diagup N \rightarrow G \diagup H$ s.t. $\varphi(gN)=gH$\\
    Well Defined: Let $xN=yN \implies xy^{-1} \in N \subseteq H$\\
    $\implies xy^{-1}H=H \implies xH=yH \implies \varphi(xN)=\varphi(yN)$\\
    Kernel: Let $gN \in$ ker$(\varphi)$ s.t. $g \in G$\\
    $\varphi(gN)=gH=H \implies g \in H \implies gN \in H \diagup N$\\
    Let $hN \in H \diagup N$ s.t. $h \in H$\\
    $\varphi(hN)=hH=H \implies hN \in$ ker$(\varphi)$\\
    Thus ker$(\varphi) = H \diagup N$\\
    Surjective: Let $gH \in G \diagup H$ s.t. $g \in G$\\
    Consider: $\varphi(gN)=gH$ thus surjective\\
    By the first isomorphism theorem $\dfrac{G \diagup N}{H \diagup N} \cong G \diagup H$
\end{enumerate}

\newpage
\subsection*{Problem 6}
\begin{proof}
    If $X,Y \unlhd G$ then $XY \leq G$
    \begin{itemize}
        \item (Closure) Let $a,b \in XY$ thus $a=xy$ and $b=ij$ for $x,i \in X$ and $yj \in Y$\\
        $ab=xyij=x(yi)j$ since $X$ is normal in $G$ we know $yiy^{-1}=x' \in X \implies yi=x'y$\\
        $=xx'yj \in XY$
        \item (Inverses) Let $a \in XY$ thus $a=xy$ for $x \in X$ and $y \in Y$\\
        $a=xy \implies a^{-1}=y^{-1}x^{-1}$ since $X$ is normal in $G$ we know $y^{-1}x^{-1}y=x' \in X \implies y^{-1}x^{-1}=x'y^{-1}$\\
        $a^{-1}=x'y^{-1} \in XY$
        \item (Identity) $e \in X \cap Y$ thus $ee=e\in XY$
    \end{itemize}
    Thus $XY \leq G$
\end{proof}
Let $G$ be order 8.\\
By lagrange's theorem $G$ can only have terms of order 1,2,4,8
\begin{enumerate}
    \item If $G$ has an element $g \in G$ s.t. order of $g$ is 8.\\
    $\ang{g} \leq G$ but since $|\ang{g}|=|G| \implies \ang{g}=G \implies G$ is cyclic thus $G \cong C_8$
    \item If $G$ does not have an element of order 8 or 4 thus all nonidentity elements have order 2.\\
    Let $a\neq b \neq e$ s.t. $a,b \in G$ thus let $X=\{e,a\}$ and $Y=\{e,b\}$\\
    $XY=\{e,a,b,ab\}$ since $XY$ has 4 elements and is obviously not cyclic $XY \cong C_2 \times C_2$ and $XY \leq G$\\
    Let $c \notin XY$ this is possible since there are 4 elements in $XY$, so there are 4 remaining elements in $G$ thus $Z=\{e,c\}$\\
    Consider: $XYZ$ since $G$ is abelian all subgroups are normal so $XY$ and $Z$ are normal in $G$ and $XY \cap Z = \{e\}$\\
    $XYZ$ is a subgroup of $G$ since $XY$ and $Z$ are normal, so its order must divide $8$ by lagrange's theorem.\\
    $XYZ$ must have $\{e,a,b,ab,c\}$ so it must have at least 5 elements, since its order must divide 8 this implies its order is 8 thus $XYZ=G$ and since $Z$ is order 2, $Z \cong C_2$\\
    thus by problem 4 $G=XYZ \cong XY \times Z \cong C_2\times C_2 \times C_2$
    \item If $G$ does not have an element of order 8 thus $G$ only has elements of order 1,2,4\\
    Let $a \in G$ s.t. ord$(a)=4$ and $X=\{e,a,a^2,a^3\}=\ang{a}$ and let $b \notin X$
    \begin{enumerate}
        \item If ord$(b)=2$ then let $Y=\{e,b\}=\ang{b}$\\
        Then $X,Y$ are both normal since they are subgroups of an abelian group and $X \cap Y = \{e\}$\\
        Also consider $XY$, $XY$ is a subgroup of $G$ since $X$ and $Y$ are normal. $XY$ must also contain $\{e,a,a^2,a^3,b\}$ so a total of 5 elements
        however the order must divide the order of $G$ so this implies $|XY|=8 \implies XY=G$ thus by problem 4, $G \cong X \times Y$\\
        and since $X$ is cyclic $X \cong C_4$ and since $Y$ is cyclic $Y \cong C_2$ thus $G \cong X \times Y \cong C_4 \times C_2$
        \item If ord$(b)=4 \implies$ ord$(b^2)=2$
        \begin{enumerate}
            \item If $b^2 \notin X$ then done by proof above in case (a)
            \item If $b^2 \in H \implies b^2 = a^2$ since these have the same order.\\
            Thus consider $ab$, $ab$ is not in $X$ since $a \in X$ and $b \notin X$\\
            $abab=a^2b^2=a^2a^2=a^4=e$ thus $ab$ is order 2 and not in $X$ thus done by proof above in case (a)
        \end{enumerate}
    \end{enumerate}
\end{enumerate}

\newpage
\subsection*{Problem 7}
\begin{proof}
    Let $G$ be a group and $H \leq G$ and $[G : H] = 2$ then $H \unlhd G$\\
    Since $[G : H] = 2$ this implies $G=gH\sqcup H$ for $g \notin H$\\
    Consider $Hg$ since $g \notin H \implies Hg \neq H$\\
    If $x \in H \implies xHx^{-1}=Hx^{-1}=H$\\
    Thus $H \unlhd G$
\end{proof}
\begin{enumerate}[(a)]
    \item If $G$ had an element of order $8$ then it would be cyclic and thus abelian\\
    If $G$ did not have an element of order 8 or 4 then it would only have elements of order 1,2 thus proven in class since all nonidentity elements have order 2 this makes $G$ abelian\\
    So $G$ must have an element $x$ of order 4\\
    By lagrange's theorem, $[G : \ang{x}] =2$ thus by proof above $\ang{x} \unlhd G$ thus $yxy^{-1}\in \ang{x}$\\
    Since $x$ is order 4 this means $yxy^{-1}$ is order 4 this $yxy^{-1}=x \text{ or } x^3$ if it is $x$ then this implies $yx=xy$ which implies G is abelian so this is not possible
    thus $yxy^{-1}=x^3$\\
    Same for $y^{-1}xy$, so it also equals $x^3$
    \item \begin{enumerate}[1.]
        \item If ord$(y)=2$ then $G=\ang{x,y \mid x^4=y^2=e,\; yx=x^3y}$\\
        Let $\varphi : G \rightarrow D_4$ s.t. $\varphi(x)=a$ where $a$ is the rotation and $\varphi(y)=b$ where $b$ is the reflection.\\
        Homomorphism: Since $x,y$ are generators only have to check $\varphi(y)\varphi(x)=\varphi(x)^3\varphi(y)$ holds and that we send same order to same order but by defintion of $\varphi$ we do send same order to same order\\
        $\varphi(y)\varphi(x)=ba=a^{3}b=\varphi(x)^3\varphi(y)$\\
        Injective:\\
        $\varphi(x^2)=a^2$\\
        $\varphi(y^2)=b^2$\\
        $\varphi(yx)=\varphi(y)\varphi(x)=ba$\\
        $\varphi(yx^2)=ba^2$\\
        $\varphi(yx^3)=ba^3$\\
        Thus injective\\
        Surjective: Let $g \in D_4$\\
        Thus $g=b^ja^i$  for $i \in \{0,1,2,3\}$ and $j \in \{0,1\}$\\
        Consider $y^jx^i$\\
        $\varphi(y^jx^i)=\varphi(y^j)\varphi(x^i)=\varphi(y)^j\varphi(x)^i=b^ja^i$ thus surjective\\
        Thus $\varphi$ is an isomorphism thus $G \cong D_4$
        \item If ord$(y)=4$ then ord$(y^2)=2$\\
        $yxyx=yyx^3x=y^2$, so order is more than 2\\
        $yx^3yx^3=yyx^4=y^2$, so order is more than 2\\
        thus the only element with order $2$ is $x^2$ thus $x^2=y^2$\\
        Let $\varphi : G \rightarrow Q$ s.t. $\varphi(x)=i$ and $\varphi(y)=j$\\
        Homomorphism: Since $x,y$ are generators only have to check order, which is the same, and that $\varphi(y)\varphi(x)\varphi(y)^{-1}=\varphi(x)^3$ and $\varphi(y)^2=\varphi(x)^2$\\
        $\varphi(y)\varphi(x)\varphi(y)^{-1}=jij^{-1}=jij^3=jkj^2=ii^2=i^3=\varphi(x)^3$\\
        $\varphi(y)^2=j^2=i^2=\varphi(x)^2$\\
        Injective:\\
        $\varphi(x^2)=\varphi(x)^2=i^2=-1$\\
        $\varphi(y^2)=\varphi(y)^2=\varphi(x)^2=-1$\\
        $\varphi(yx)=\varphi(y)\varphi(x)=ji=-k$\\
        $\varphi(yx^2)=j-1=-j$\\
        $\varphi(yx^3)=j(-i)=k$\\
        Thus injective\\
        Surjective: Let $g \in Q$ thus $g=j^ai^b$ for $a \in \{0,1,2,3\}$ and $b \in \{0,1\}$\\
        Consider $y^ax^b$\\
        $\varphi(y^ax^b)=\varphi(y^a)\varphi(x^b)=\varphi(y)^a\varphi(x)^b=j^ai^b$\\
        Thus $\varphi$ is an isomorphism thus $G \cong Q$
    \end{enumerate}
    \item \begin{tikzpicture}
        \matrix (m) [matrix of nodes, row sep=1cm, column sep=1cm]{
            & & $Q$ & & \\
            & $\langle i \rangle$ & $\langle j \rangle$ & $\langle k \rangle$& \\
            & & $\langle -1 \rangle$ & & \\
            & & $\{ 1 \}$ & & \\
        };
      
        \draw (m-1-3) -- (m-2-2);
        \draw (m-1-3) -- (m-2-3);
        \draw (m-1-3) -- (m-2-4);
        \draw (m-2-2) -- (m-3-3);
        \draw (m-2-3) -- (m-3-3);
        \draw (m-2-4) -- (m-3-3);
        \draw (m-3-3) -- (m-4-3);
      \end{tikzpicture}
      \phantom{text}\\
Let $H$ be a subgroup of $Q$ by Lagrange's Theorem the order of $H$ can only be $1,2,4,8$

Case 1: \(H\) is order 1
\begin{itemize}
    \item The only subgroup of order 1 is the trivial subgroup \( \{e\} \).
    \item Since \( \{e\} \) is already on our list, \( H \) cannot be of order 1 unless it is \( \{e\} \), which is already accounted for.
\end{itemize}

Case 2: \(H\) is order 2
\begin{itemize}
    \item If $H$ is order 2, it must consist of $e$ and one other element $g$, where $g^2=e$.
    \item In $Q$ the elements with order 2 are only $-1$\\
    Therefore, any subgroup \( H \) of order 2 must be $\{1,-1\}$.
\end{itemize}

Case 3: \(H\) is order 4
\begin{itemize}
    \item If $H \cong C_4 \implies H$ has an element of order 4.\\
    Thus, elements of order 4 are $i,-i,j,-j,k,-k$ and since
    \item $\ang{i}=\ang{-i}$
    \item $\ang{j}=\ang{-j}$
    \item $\ang{k}=\ang{-k}$\\
    Thus $H$ is one of these
    \item If $H \cong C_2\times C_2 \implies H$ has only elements of order 2.\\
    However, there are not enough elements of order 2 for this to happen.
\end{itemize}

Case 4: \(H\) is order 8
\begin{itemize}
    \item If \( H \) is order 8, it must be the whole group \( Q \).
    \item Therefore, \( H \) would be \( Q \), which is already on our list.
\end{itemize}
\phantom{text}\\
In each case, we've shown that any possible subgroup \( H \) of \( Q \) must be one of the subgroups already listed. Therefore, no additional subgroups can exist, and our list of subgroups is complete.\\
By the proof below since $Z(Q)=\ang{-1}$ this means it is normal and $\{e\}$ and the full group are always normal.\\
For $\ang{i}$ we only have to check for conugacy with the generators\\
\begin{itemize}
    \item For $i$ we know that conguates with all elements in $\ang{i}$ since subgroup closure.
    \item For $j$, $jij^{-1}=ji(-j)=(-j)ij=j^{-1}ij=kj=-i \in \ang{i}$
    \item For $k$, $kik^{-1}=ki(-k)=(-k)ik=k^{-1}ik=-ijiij=-ij(-1)j=-(-i)j^2=-i \in \ang{i}$
\end{itemize}
Thus $\ang{i}$ is normal and similar proof for $\ang{j}$ and $\ang{k}$ thus all subgroups of $Q$ are normal.
\begin{itemize}
    \item $1 \in Z(Q)$
    \item $-1$, let $x \in Q$
    $(-1)x=x(-1)=-x$ thus $-1 \in Z(Q)$
    \item $i$, $ij=k$ and $ji=-k$
    \item $-i$, $(-i)j=-k$ and $j(-i)=k$
    \item $j$, $ij=k$ and $ji=-k$
    \item $-j$, $i(-j)=-k$ and $(-j)i=k$
    \item $k$, $ki=j$ and $ik=-j$
    \item $-k$, $-ki=-j$ and $i(-k)=j$
    \item Thus $Z(Q)=\{1,-1\}$
\end{itemize}
\end{enumerate}

\newpage
\subsection*{Problem 8}
\begin{itemize}
    \item (Closure) Let $A,B\in H$
    \[
    A = \begin{pmatrix}
    1 & a_1 & b_1 \\
    0 & 1 & c_1 \\
    0 & 0 & 1
    \end{pmatrix}, \quad B = \begin{pmatrix}
    1 & a_2 & b_2 \\
    0 & 1 & c_2 \\
    0 & 0 & 1
    \end{pmatrix}
    \]

    \[
    AB = \begin{pmatrix}
    1 & a_1 & b_1 \\
    0 & 1 & c_1 \\
    0 & 0 & 1
    \end{pmatrix} 
    \begin{pmatrix}
    1 & a_2 & b_2 \\
    0 & 1 & c_2 \\
    0 & 0 & 1
    \end{pmatrix}
    = \begin{pmatrix}
    1 & a_1 + a_2 & b_1 + a_1 c_2 + b_2 \\
    0 & 1 & c_1 + c_2 \\
    0 & 0 & 1
    \end{pmatrix}
    \]
    thus $AB \in H$
    \item (Identity) $I \in H$
    \item (Inverses) Let $A \in H$

    \[
    A = \begin{pmatrix} 
    1 & a & b \\
    0 & 1 & c \\
    0 & 0 & 1 
    \end{pmatrix}, \quad A^{-1} = \begin{pmatrix}
    1 & -a & ac - b \\
    0 & 1 & -c \\
    0 & 0 & 1
    \end{pmatrix}
    \]

    \[
    AA^{-1} = \begin{pmatrix}
    1 & a & b \\
    0 & 1 & c \\
    0 & 0 & 1
    \end{pmatrix} 
    \begin{pmatrix}
    1 & -a & ac-b \\
    0 & 1 & c_2 \\
    0 & 0 & 1
    \end{pmatrix}
    = \begin{pmatrix}
    1 & a-a & b - ac + ac - b \\
    0 & 1 & c - c \\
    0 & 0 & 1
    \end{pmatrix} = I
    \]
    \[
    A^{-1}A = \begin{pmatrix}
    1 & -a & ac-b \\
    0 & 1 & c_2 \\
    0 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix}
    1 & a & b \\
    0 & 1 & c \\
    0 & 0 & 1
    \end{pmatrix}
    = \begin{pmatrix}
    1 & a-a & b - ac + ac - b \\
    0 & 1 & c - c \\
    0 & 0 & 1
    \end{pmatrix} = I
    \]
    Thus $H$ is a subgroup
\end{itemize}

Let $A \in H$ and $B \in K$
\[
    A = \begin{pmatrix}
    1 & a & b \\
    0 & 1 & c \\
    0 & 0 & 1
    \end{pmatrix}, \quad B = \begin{pmatrix}
    1 & 0 & g \\
    0 & 1 & 0 \\
    0 & 0 & 1
    \end{pmatrix}
    \]

    \[
    ABA^{-1} = \begin{pmatrix}
    1 & a & b \\
    0 & 1 & c \\
    0 & 0 & 1
    \end{pmatrix} 
    \begin{pmatrix}
    1 & 0 & g \\
    0 & 1 & 0 \\
    0 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix}
    1 & -a & ac - b \\
    0 & 1 & -c \\
    0 & 0 & 1
    \end{pmatrix}
    = \begin{pmatrix}
    1 & a & b+g \\
    0 & 1 & c \\
    0 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix}
    1 & -a & ac - b \\
    0 & 1 & -c \\
    0 & 0 & 1
    \end{pmatrix}
    \]
    \[
    =\begin{pmatrix}
    1 & 0 & g \\
    0 & 1 & 0 \\
    0 & 0 & 1
    \end{pmatrix}
    \]
    Thus $ABA^{-1} \in K$ thus $K \unlhd H$\\
    Let $h \in H$, consider $hK$\\
    \[
    hK = \left\{ \begin{pmatrix}
        1 & x & y \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}
        \cdot
        \begin{pmatrix}
        1 & 0 & g \\
        0 & 1 & 0 \\
        0 & 0 & 1
        \end{pmatrix}=
        \begin{pmatrix}
        1 & x & g+y \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    Thus $A=B$ for $A,B \in H \diagup K$ if $a_{12}=b_{12}$ and $a_{23}=b_{23}$, $a_{ij}$ is the $(i,j)$ component.\\
    Thus
    \[
    H \diagup K = \left\{ \left\{\begin{pmatrix}
        1 & x & g \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}  : x,z \in \mathbb{R} \right\}
    \]
    Thus let $\varphi : H \diagup K \rightarrow \R^2$ s.t. for $A \in H \diagup K$ and $x,z \in \R$
    \[
    A = \left\{ 
        \begin{pmatrix}
        1 & x & g \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    \[\varphi(A)=(x,z)\]
    Homomorphism: Let $A,B \in H \diagup K$ s.t.
    \[
    A = \left\{ 
        \begin{pmatrix}
        1 & x & g \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\} \quad B = \left\{ 
        \begin{pmatrix}
        1 & a & g \\
        0 & 1 & b \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    for $x,z,a,b \in \R$
    \[
    AB = \left\{ 
        \begin{pmatrix}
        1 & x+a & g \\
        0 & 1 & z+b \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    $\varphi(AB)=(x+a,z+b)=(x,z)+(a,b)=\varphi(A)\varphi(B)$\\
    Injective: Let $A \in$ ker$(H \diagup K)$ for $x,z \in \R$
    \[
    A = \left\{ 
        \begin{pmatrix}
        1 & x & g \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    $\varphi(A)=(x,y)=(0,0) \implies x=0$ and $y=0$ thus
    \[
    K=A = \left\{ 
        \begin{pmatrix}
        1 & 0 & g \\
        0 & 1 & 0 \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    thus the kernel is trivial thus $\varphi$ is injective\\
    Surjective: Let $(x,z) \in \R^2$ s.t. $x \in \R$ and $z \in \R$\\
    Consider: 
    \[
    A = \left\{ 
        \begin{pmatrix}
        1 & x & g \\
        0 & 1 & z \\
        0 & 0 & 1
        \end{pmatrix}  : g \in \mathbb{R} \right\}
    \]
    $\varphi(A)=(x,z)$ thus surjective\\
    Thus $H \diagup K \cong \R^2$\\
    Let $A \in H$ and $B \in Z(H)$
    \[
    A = \begin{pmatrix}
    1 & x & y \\
    0 & 1 & z \\
    0 & 0 & 1
    \end{pmatrix}, \quad
    B = \begin{pmatrix}
    1 & x' & y' \\
    0 & 1 & z' \\
    0 & 0 & 1
    \end{pmatrix}
    \]
        
    \[
    AB = \begin{pmatrix}
    1 & x & y \\
    0 & 1 & z \\
    0 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix}
    1 & x' & y' \\
    0 & 1 & z' \\
    0 & 0 & 1
    \end{pmatrix}
    = \begin{pmatrix}
    1 & x + x' & y + x'z + y' \\
    0 & 1 & z + z' \\
    0 & 0 & 1
    \end{pmatrix}
    \]
    \[
    BA = \begin{pmatrix}
    1 & x' & y' \\
    0 & 1 & z' \\
    0 & 0 & 1
    \end{pmatrix}
    \begin{pmatrix}
    1 & x & y \\
    0 & 1 & z \\
    0 & 0 & 1
    \end{pmatrix}
    = \begin{pmatrix}
    1 & x + x' & y' + xz' + y \\
    0 & 1 & z + z' \\
    0 & 0 & 1
    \end{pmatrix}
    \]
    since $B \in Z(H) \implies AB=BA$ thus $y+x'z+y'=y'+xz'+y \implies x'z=xz' \implies x'z-xz'=0$
    \begin{enumerate}
        \item If $x' = 0$ then $xz'=0 \implies z'=0$ since $x$ could be anything
        \item If $z'=0$ then similar reasoning to above $ \implies x'=0$
        \item If $x' \neq 0 \implies z' \neq 0$ thus $\frac{z}{z'}=\frac{x}{x'}$ however since this implies that $z'$ and $x'$ depend on what $z,x$ are this solution will not work.
    \end{enumerate}
    Thus, $B \in K$\\
    Show above for $A \in H$ and $B \in K$, $ABA^{-1}=B \implies AB=BA$ thus $B \in Z(H)$\\
    Thus $Z(G)=K$
\end{document}